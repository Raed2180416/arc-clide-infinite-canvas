{
  "schemaVersion": "arc-atlas-scope-policy-ledger-v1",
  "authority": "atlas-maintainer-reviewed-scope-classification-no-ignored-content-inspection",
  "principle": "A policy explains why an exact ignored path is outside the source corpus. It does not claim to know or validate the ignored bytes.",
  "policies": [
    { "id": "installed-node-dependencies", "match": { "ignorePattern": "node_modules/" }, "classification": "installed-dependencies", "rationale": "Package installations are reproducible dependency state governed by manifests and lockfiles, not repository-authored source.", "contentPolicy": "metadata-only" },
    { "id": "nested-installed-node-dependencies", "match": { "ignorePattern": "node_modules" }, "classification": "installed-dependencies", "rationale": "This nested package installation is generated from package metadata and is not repository-authored source.", "contentPolicy": "metadata-only" },
    { "id": "python-virtual-environment", "match": { "ignorePattern": "venv/" }, "classification": "installed-runtime-environment", "rationale": "The Python virtual environment is installed interpreter and dependency state, not repository-authored source.", "contentPolicy": "metadata-only" },
    { "id": "rust-target-output", "match": { "ignorePattern": "target/" }, "classification": "compiler-build-output", "rationale": "Rust target trees are compiler products derived from source and build configuration.", "contentPolicy": "metadata-only" },
    { "id": "kilo-agent-state", "match": { "ignorePattern": ".kilo/" }, "classification": "external-agent-local-state", "rationale": "Kilo stores mutable tool-local state here; it is not source or intent authority.", "contentPolicy": "metadata-only" },
    { "id": "claude-science-local-data", "match": { "ignoreSourcePathSuffix": "claude-science-data/.gitignore", "ignorePattern": "*" }, "classification": "external-research-local-data", "rationale": "The subtree owns a closed local-data boundary and excludes its mutable research payload from source control.", "contentPolicy": "metadata-only" },
    { "id": "benchmark-generated-state", "match": { "ignorePattern": "/benchmarks/*" }, "classification": "benchmark-generated-or-private-state", "rationale": "Ignored benchmark descendants are generated, private, or experimental state; tracked benchmark definitions remain in the visible corpus.", "contentPolicy": "metadata-only" },
    { "id": "local-runtime-state", "match": { "ignorePattern": "local/" }, "classification": "local-runtime-or-build-state", "rationale": "Directories named local under this ignore contract hold machine-local runtime or build material rather than portable source.", "contentPolicy": "metadata-only" },
    { "id": "audit-working-state", "match": { "ignorePattern": ".audit/" }, "classification": "audit-working-state", "rationale": "Mutable audit scratch state is excluded; adjudicated findings and registered receipts remain visible separately.", "contentPolicy": "metadata-only" },
    { "id": "generated-grammars", "match": { "ignorePattern": "grammars/" }, "classification": "generated-grammar-artifacts", "rationale": "These grammar artifacts are generated runtime products rather than their code-owned source definitions.", "contentPolicy": "metadata-only" },
    { "id": "crush-local-state", "match": { "ignoreSourcePathSuffix": ".crush/.gitignore", "ignorePattern": "*" }, "classification": "external-agent-local-state", "rationale": "Crush owns this mutable local state boundary; it has no repository intent authority.", "contentPolicy": "metadata-only" },
    { "id": "python-bytecode-cache", "match": { "ignorePattern": "__pycache__/" }, "classification": "interpreter-cache", "rationale": "Python bytecode caches are derived interpreter output.", "contentPolicy": "metadata-only" },
    { "id": "script-python-bytecode-cache", "match": { "ignorePattern": "scripts/__pycache__/" }, "classification": "interpreter-cache", "rationale": "Python bytecode caches under scripts are derived interpreter output.", "contentPolicy": "metadata-only" },
    { "id": "codebase-memory-cache", "match": { "ignorePattern": ".codebase-memory/" }, "classification": "advisory-derived-index", "rationale": "Codebase Memory state is a regenerable advisory index and never source, intent, or product authority.", "contentPolicy": "metadata-only" },
    { "id": "script-ctxo-state", "match": { "ignorePattern": "scripts/.ctxo/" }, "classification": "tool-local-state", "rationale": "This CTXO subtree is mutable tool-local state, not repository source.", "contentPolicy": "metadata-only" },
    { "id": "absolute-human-state", "match": { "ignorePattern": ".absolute-human/" }, "classification": "external-tool-local-state", "rationale": "Absolute Human tool state is local and does not define repository behavior or intent.", "contentPolicy": "metadata-only" },
    { "id": "ctxo-state", "match": { "ignorePattern": ".ctxo/" }, "classification": "tool-local-state", "rationale": "CTXO mutable state is excluded from the portable source boundary.", "contentPolicy": "metadata-only" },
    { "id": "database-sidecars", "match": { "ignorePattern": "*.db-*" }, "classification": "database-transient-sidecar", "rationale": "Database journals, locks, and sidecars are transient runtime state.", "contentPolicy": "metadata-only" },
    { "id": "local-databases", "match": { "ignorePattern": "*.db" }, "classification": "local-database-state", "rationale": "Ignored database files are mutable local state; registered schemas and migrations remain visible source.", "contentPolicy": "metadata-only" },
    { "id": "aider-local-state", "match": { "ignorePattern": ".aider*" }, "classification": "external-agent-history-or-cache", "rationale": "Aider histories, tags, and local UI state are not repository source or owner authority.", "contentPolicy": "metadata-only" },
    { "id": "test-working-tree", "match": { "ignorePattern": "test/" }, "classification": "local-test-working-state", "rationale": "This ignored test subtree is local working state; tracked test definitions remain visible.", "contentPolicy": "metadata-only" },
    { "id": "antigravity-local-state", "match": { "ignorePattern": ".antigravitycli/" }, "classification": "external-agent-local-state", "rationale": "Antigravity CLI state is mutable local tool state.", "contentPolicy": "metadata-only" },
    { "id": "claude-local-settings", "match": { "ignorePattern": "**/.claude/settings.local.json" }, "classification": "private-machine-configuration", "rationale": "Machine-local Claude settings may contain private configuration and are deliberately outside portable source.", "contentPolicy": "never-read" },
    { "id": "environment-secrets", "match": { "ignorePattern": ".env" }, "classification": "private-environment-configuration", "rationale": "Environment files may contain credentials or machine-local configuration and must not enter the atlas source corpus.", "contentPolicy": "never-read" },
    { "id": "environment-variants", "match": { "ignorePattern": ".env.*" }, "classification": "private-or-local-environment-configuration", "rationale": "Environment variants are treated as private or machine-local configuration unless explicitly tracked elsewhere.", "contentPolicy": "never-read" },
    { "id": "logicstamp-local-state", "match": { "ignorePattern": ".logicstamp/" }, "classification": "external-tool-local-state", "rationale": "Logicstamp stores mutable tool-local state here.", "contentPolicy": "metadata-only" },
    { "id": "opencode-ignore-control", "match": { "ignoreSourcePathSuffix": ".opencode/.gitignore", "ignorePattern": ".gitignore" }, "classification": "nested-tool-packaging-state", "rationale": "The nested OpenCode package ignores its copied packaging control file inside local tool state.", "contentPolicy": "metadata-only" },
    { "id": "opencode-lockfile", "match": { "ignoreSourcePathSuffix": ".opencode/.gitignore", "ignorePattern": "package-lock.json" }, "classification": "nested-tool-packaging-state", "rationale": "This lockfile belongs to the ignored local OpenCode package installation rather than the repository package boundary.", "contentPolicy": "metadata-only" },
    { "id": "opencode-package", "match": { "ignoreSourcePathSuffix": ".opencode/.gitignore", "ignorePattern": "package.json" }, "classification": "nested-tool-packaging-state", "rationale": "This manifest belongs to the ignored local OpenCode package installation rather than the repository package boundary.", "contentPolicy": "metadata-only" },
    { "id": "playwright-logs", "match": { "ignorePattern": "*.log" }, "classification": "runtime-log", "rationale": "Runtime logs are mutable observations and become evidence only through an explicitly registered receipt.", "contentPolicy": "metadata-only" },
    { "id": "scratch-driver-state", "match": { "ignorePattern": ".scratch-driver-test/" }, "classification": "test-scratch-state", "rationale": "This driver scratch directory is ephemeral test state.", "contentPolicy": "metadata-only" },
    { "id": "serena-cache", "match": { "ignoreSourcePathSuffix": ".serena/.gitignore", "ignorePattern": "/cache" }, "classification": "advisory-tool-cache", "rationale": "Serena cache state is regenerable advisory data, not source authority.", "contentPolicy": "metadata-only" },
    { "id": "serena-memories", "match": { "ignoreSourcePathSuffix": ".serena/.gitignore", "ignorePattern": "/memories" }, "classification": "tool-local-memory", "rationale": "Serena-local memories are outside the registered repository canon and cannot override it.", "contentPolicy": "metadata-only" },
    { "id": "tui-shot", "match": { "ignorePattern": "apps/arc-tui/shot.png" }, "classification": "generated-visual-artifact", "rationale": "The TUI screenshot is generated render output; the renderer and visual tests remain visible source.", "contentPolicy": "metadata-only" },
    { "id": "agent-map-json", "match": { "ignorePattern": "docs/agent-map/*.json" }, "classification": "generated-documentation-projection", "rationale": "Agent-map JSON is a regenerable projection and not canonical documentation authority.", "contentPolicy": "metadata-only" },
    { "id": "agent-map-markdown", "match": { "ignorePattern": "docs/agent-map/*.md" }, "classification": "generated-documentation-projection", "rationale": "Agent-map Markdown is a regenerable projection and not canonical documentation authority.", "contentPolicy": "metadata-only" },
    { "id": "local-model-assets", "match": { "ignorePattern": "/models" }, "classification": "external-model-artifact", "rationale": "Model weights and local model artifacts are external binaries bound through runtime admission rather than source control.", "contentPolicy": "metadata-only" },
    { "id": "cgc-ignore-control", "match": { "ignorePattern": "scripts/.cgcignore" }, "classification": "tool-generated-ignore-control", "rationale": "This tool-specific ignore control is generated or installed working state under scripts.", "contentPolicy": "metadata-only" },
    { "id": "script-ignore-control", "match": { "ignorePattern": "scripts/.gitignore" }, "classification": "tool-generated-ignore-control", "rationale": "This nested ignore control belongs to installed or generated script tooling state.", "contentPolicy": "metadata-only" },
    { "id": "remember-local-state", "match": { "ignoreSourcePathSuffix": ".remember/.gitignore", "ignorePattern": "*" }, "classification": "external-memory-tool-state", "rationale": "Remember tool state is local and cannot override registered repository memory or canon.", "contentPolicy": "metadata-only" }
  ]
}
